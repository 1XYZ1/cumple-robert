---
import Lightbox from "../../components/Lightbox"
import Layout from "../../layouts/Layout.astro"
import { jugadores } from "../../data/jugadores";
title: "Fútbol"

const robertImages = [
  {
    src: "/images/robert-fut.jpeg",
    alt: "Robert jugando",
  },

];

const broskyImages = [
  {
    src: "/images/brosky.jpeg",
    alt: "Brosky FC jugando",
  },

];

const portalesImages = [
  {
    src: "/images/portales.jpeg",
    alt: "Portales FC",
  },

];


---

<Layout>

  <a
  href="/#actividades"
  class="fixed top-8 left-8 p-4 bg-gray-900/50 hover:bg-gray-900/80 text-white rounded-full backdrop-blur-sm transition-all hover:scale-110 z-50 border border-white/10"
>
  <i class="fas fa-home text-xl"></i>
</a>
  <section class="py-16 bg-gradient-to-b from-gray-900 via-gray-800 to-gray-900">
    <div class="max-w-7xl mx-auto px-4">
      <div class="mb-12 text-center space-y-4">
        <h2 class="text-4xl font-bold text-white font-montserrat relative inline-block">
          Participantes
        </h2>
        <p class="text-gray-400 max-w-2xl mx-auto">
          Conoce a los jugadores que formarán parte de este increíble torneo
        </p>
      </div>
      <!-- SVG de la cancha (sin cambios) -->
      <div class="mx-auto my-8 w-2/3">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 500 300"
          width="100%"
          height="auto"
          class="mx-auto my-8"
        >
            <!-- Fondo del Campo -->
            <rect x="0" y="0" width="500" height="300" fill="#5cb85c" />

            <!-- Líneas de Marcado -->
            <rect x="10" y="10" width="480" height="280" fill="none" stroke="#ffffff" stroke-width="2" />
            <line x1="250" y1="10" x2="250" y2="290" stroke="#ffffff" stroke-width="2" />
            <circle cx="250" cy="150" r="40" fill="none" stroke="#ffffff" stroke-width="2" />
            <circle cx="250" cy="150" r="2" fill="#ffffff" />

            <!-- Área de Penal Equipo 1 -->
            <rect x="10" y="85" width="60" height="130" fill="none" stroke="#ffffff" stroke-width="2" />
            <rect x="10" y="115" width="20" height="70" fill="none" stroke="#ffffff" stroke-width="2" />
            <circle cx="30" cy="150" r="2" fill="#ffffff" />

            <!-- Área de Penal Equipo 2 -->
            <rect x="430" y="85" width="60" height="130" fill="none" stroke="#ffffff" stroke-width="2" />
            <rect x="480" y="115" width="20" height="70" fill="none" stroke="#ffffff" stroke-width="2" />
            <circle cx="470" cy="150" r="2" fill="#ffffff" />

            <!-- Arcos -->
            <line x1="10" y1="130" x2="0" y2="130" stroke="#ffffff" stroke-width="2" />
            <line x1="10" y1="170" x2="0" y2="170" stroke="#ffffff" stroke-width="2" />
            <line x1="490" y1="130" x2="500" y2="130" stroke="#ffffff" stroke-width="2" />
            <line x1="490" y1="170" x2="500" y2="170" stroke="#ffffff" stroke-width="2" />
          </svg>
        </div>

        <!-- Sección de Participantes -->
        <section class="py-12 bg-gray-900">
          <div class="max-w-6xl mx-auto px-4">
            <h2 class="text-3xl font-bold text-center mb-8 text-white font-montserrat">Participantes</h2>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
              {jugadores.map((jugador) => (
                <div class="p-1 rounded-3xl h-[600px]" style={`
                  background: linear-gradient(45deg,
                    ${jugador.equipoFavorito.colores.primario},
                    ${jugador.equipoFavorito.colores.secundario}
                  );
                `}>
                  <div class="rounded-2xl overflow-hidden bg-gray-900 h-full">
                    {/* Header con imagen */}
                    <div class="relative h-[300px] overflow-hidden">
                      <img
                        src={jugador.imagen}
                        alt={jugador.nombre}
                        class="w-full h-full object-cover"
                      />
                      <div class="absolute inset-0 bg-gradient-to-t from-gray-900 to-transparent"></div>
                      <div class="absolute bottom-0 left-0 w-full p-4 text-white">
                        <div class="flex items-center justify-between">
                          <div>
                            <h3 class="text-2xl font-bold">{jugador.nombre}</h3>
                            <p class="text-lg italic opacity-90">"{jugador.apodo}"</p>
                          </div>
                          <img
                            src={jugador.equipoFavorito.escudo}
                            alt={jugador.equipoFavorito.nombre}
                            class="w-16 h-16 rounded-full p-1 bg-white/10"
                          />
        </div>
      </div>
    </div>

    {/* Info del jugador */}
    <div class="p-6 space-y-6">
      {/* Datos básicos */}
      <div class="grid grid-cols-2 gap-4">
        <div class="flex items-center gap-2">
          <i class="fas fa-tshirt text-lg" style={`color: ${jugador.equipoFavorito.colores.primario}`}></i>
          <span class="text-white">#{jugador.numero}</span>
        </div>
        <div class="flex items-center gap-2">
          <i class="fas fa-running text-lg" style={`color: ${jugador.equipoFavorito.colores.primario}`}></i>
          <span class="text-white">{jugador.posicion}</span>
        </div>
        <div class="flex items-center gap-2">
          <i class="fas fa-birthday-cake text-lg" style={`color: ${jugador.equipoFavorito.colores.primario}`}></i>
          <span class="text-white">{jugador.edad} años</span>
        </div>
        <div class="flex items-center gap-2">
          <i class="fas fa-users text-lg" style={`color: ${jugador.equipoFavorito.colores.primario}`}></i>
          <span class="text-white">{jugador.equipo}</span>
        </div>
      </div>

      {/* Skills */}
      <div class="space-y-3">
        <h4 class="text-white font-semibold mb-2">Estadísticas</h4>
        <div class="space-y-3">
          {Object.entries(jugador.skills).map(([skill, value]) => (
            <div class="flex items-center gap-3">
              <span class="text-gray-400 text-sm w-20 capitalize">{skill}</span>
              <div class="flex-1 bg-gray-700 rounded-full h-2">
                <div
                  class="h-full rounded-full"
                  style={`
                    width: ${value}%;
                    background: linear-gradient(90deg,
                      ${jugador.equipoFavorito.colores.primario},
                      ${jugador.equipoFavorito.colores.secundario}
                    );
                  `}
                ></div>
              </div>
              <span class="text-white text-sm w-8">{value}</span>
            </div>
          ))}
        </div>
      </div>

      {/* Estilo de juego */}
      <div class="inline-block px-3 py-1 rounded-full" style={`
        background: linear-gradient(90deg,
          ${jugador.equipoFavorito.colores.primario}40,
          ${jugador.equipoFavorito.colores.secundario}40
        );
      `}>
        <span class="text-white text-sm">{jugador.estilo}</span>
      </div>
    </div>
  </div>
</div>
              ))}
            </div>
          </div>
        </section>

        <!-- Tarjetas de Jugadores/Partidos -->

        <div class="grid gap-6 grid-cols-1 md:grid-cols-2 lg:grid-cols-3 mt-12">
          <Lightbox
            images={robertImages}
            mainImage={robertImages[0]}
            title="Robert, el cumpleañero"
            description="Destacando con jugadas increíbles"
            client:visible
          />
          <Lightbox
            images={broskyImages}
            mainImage={broskyImages[0]}
            title="Brosky FC"
            description="Al fin ganan algo"
            client:visible
          />
          <Lightbox
            images={portalesImages}
            mainImage={portalesImages[0]}
            title="Portales FC"
            description="Los mejores momentos"
            client:visible
          />
        </div>




        {/* Mensaje para Robert */}
  <section class="py-16 bg-gradient-to-b from-gray-900 to-gray-800">
    <div class="max-w-3xl mx-auto px-4 text-center">
      <h2 class="text-3xl font-bold mb-6 text-white">Para Nuestro Crack ⚽</h2>
      <p class="text-gray-400 text-lg">
        De todos tus amigos del fútbol, gracias por tantos partidos, risas y momentos inolvidables.
        ¡Que este día sea tan especial como los goles que has marcado en nuestros corazones!
      </p>
    </div>
  </section>

      <!-- Próximamente -->
      <div class="my-12 text-center">
        <h2 class="text-3xl font-bold mb-4 text-green-700">Fotos Destacadas: Próximamente</h2>
        <p class="text-gray-700">
          Después del evento, subiremos las mejores jugadas y momentos inolvidables.
        </p>
      </div>
    </div>
  </section>


</Layout>